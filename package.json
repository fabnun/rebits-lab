{
    "private": true,
    "scripts": {
        "install": "composer install --ignore-platform-reqs",
        "migrate": "php artisan migrate",
        "encryption": "php artisan key:generate",
        "serve": "php artisan serve",
        "test": "clear && php artisan test",
        "dev": "vite",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "lodash": "^4.17.19",
        "postcss": "^8.1.14",
        "vite": "^4.0.0"
    }
}
